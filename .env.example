# Exemplo de variáveis de ambiente para deploy no Render
# Copie este arquivo e configure no Render Dashboard > Environment Variables

# Gere uma SECRET_KEY única usando:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=sua-chave-secreta-super-segura-aqui

# Modo de debug (SEMPRE False em produção)
DEBUG=False

# Hosts permitidos (adicione seu domínio do Render)
ALLOWED_HOSTS=seu-app.onrender.com,www.seu-app.onrender.com

# URL do banco de dados PostgreSQL (copie do Render PostgreSQL > Internal Database URL)
DATABASE_URL=postgresql://user:password@host:port/database

# Versão do Python (já configurada no runtime.txt, mas pode adicionar aqui também)
PYTHON_VERSION=3.13.0
